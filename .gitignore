# Tauri / Rust
app/src-tauri/target/
app/src-tauri/gen/

# Node
node_modules/
dist/
.next/
app/tmp/
# Prepared video-editor runtime payloads are large, platform-specific release
# inputs. Keep only the tracked policy/README; stage one verified target payload
# locally with app/scripts/prepare-video-runtime.mjs.
app/src-tauri/video-runtime/*/

# OS
.DS_Store
*.log
build.log

# Editor
.vscode/
.idea/

# Local release credential pack. Contains private keys and passwords.
/cpa-v2-release/

# Server has its own git repo — track via submodule later if desired
Server/.git/
Server/node_modules/

# Claude Code harness
.claude/
.codex/run/

# Local isolated implementation worktrees
.worktrees/
