{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Updater permissions are scoped to the main app window only.",
  "windows": ["main"],
  "permissions": [
    "updater:allow-check",
    "updater:allow-download-and-install",
    "process:allow-restart"
  ]
}
